# チートシート(スタイル)

## 目次

- [目次](#目次)

## テーブル

### 横スクロール

```css
#ViewModeContainer {
  overflow-y: auto;
}

table#Grid > thead > tr > th, table#Grid > tbody > tr > td {
  white-space: nowrap;
}
```

## 一覧表の画像を縮小

```css
img {
  max-width: 150px;
}
```
